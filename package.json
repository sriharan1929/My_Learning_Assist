{
  "name": "my-learning-os",
  "private": true,
  "version": "2.0.0",
  "workspaces": ["client", "server", "shared"],
  "scripts": {
    "dev": "concurrently -n client,server -c yellow,red \"npm run dev -w client\" \"npm run dev -w server\"",
    "build": "npm run build -w client",
    "test": "npm run test -w server && npm run test -w client",
    "lint": "npm run lint -w client && npm run lint -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
